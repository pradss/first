
const arr = ['Lewandowski', 'Gnarby', 'Lewandowski', 'Hummels'];

const scored = {};
for (const element of arr) {
  if (scored[element]) {
    scored[element] += 1;
  } else {
    scored[element] = 1;
  }
}
console.log(scored);
